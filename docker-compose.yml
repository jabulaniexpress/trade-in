services:
    dev:
        build: .
        ports:
            - "5173:5173"
        volumes:
            - .:/app
            - /app/node_modules
    build:
        profiles: ["build"]
        build: .
        volumes:
            - .:/app
            - /app/node_modules
        command: sh -c "npm install && npm run build"
